# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(raw_ioTaskLib)
ADD_LIBRARY(${RAW_IO_TASKLIB_NAME} SHARED
    ${RAW_IO_TASKLIB_SOURCES})
add_dependencies(${RAW_IO_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${RAW_IO_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${RAW_IO_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${RAW_IO_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${RAW_IO_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${RAW_IO_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${RAW_IO_TASKLIB_INTERFACE_LIBRARIES}")


INSTALL(TARGETS ${RAW_IO_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${RAW_IO_TASKLIB_HEADERS}
    DESTINATION include/orocos/raw_io)

